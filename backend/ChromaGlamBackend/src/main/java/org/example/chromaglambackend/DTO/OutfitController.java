package org.example.chromaglambackend.DTO;

import org.example.chromaglambackend.cg_service.UserService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;


@RestController
@RequestMapping("/api/outfit_items")
public class OutfitController
{
    /*private final OutfitService outfitService;

    public OutfitController(OutfitService outfitService) {outfitService=outfitService;}

    //outfit generating will call an internal service which will return the response of the model to the request
    @PostMapping(value = "/recommendation", consumes = "application/json")
    public ResponseEntity<HttpStatus> outfit_generating(@RequestParam int outfit_id)
    {

    }
     */
}
